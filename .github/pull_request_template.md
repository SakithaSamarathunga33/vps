## What does this PR do?

<!-- Short description of the change and why it's needed. Link related issues: Fixes #123 -->

## Type of change

<!-- The commit prefix drives the auto-release version: feat = minor, fix/chore = patch -->

- [ ] 🐛 Bug fix (`fix:`)
- [ ] ✨ New feature (`feat:`)
- [ ] 📝 Docs / chore (`chore:`, `docs:`)
- [ ] 💥 Breaking change (`BREAKING CHANGE` in commit body)

## Checklist

- [ ] The stack builds: `docker compose -f docker-compose.yml -f docker-compose.standalone.yml build`
- [ ] Go backend compiles: `cd backend && go build ./...`
- [ ] I tested the change in a running stack
- [ ] UI change → screenshot/clip attached below

## Screenshots (if UI)
